/* 동네이야기 공용 UI 헬퍼 — [1006] 본체는 lib/town/shared.ts(순수 .ts, 테스트 가능).
   여기는 기존 import 경로(../shared)를 위한 재수출만 남긴다. */
export {
  seedGradient,
  seedCoverHeight,
  newsImageUrl,
  faviconUrl,
  hostOf,
  relativeTime,
  maskNoteAuthor,
} from "@/lib/town/shared";
