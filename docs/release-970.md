# 970 — 세밀 감사 개선 128건 (2026-09-06)

소유자 요청: "디테일한 부분, 디자인, 기능, 구성, 빈공간, 허점, 오류 등에 대해 세밀하게 분석해서
개선". 로컬 969 빌드를 모바일(Pixel 5)·데스크탑·다크 모드로 46+44+36 화면 700여 장 캡처하고
코드와 라이브(966) SSR 을 대조한 감사 3편(A 셸·구독·인증·지원·법적 42건, B 노트·단지·지도·분석
46건, C 동네·전문가·마이·알림·메타 46건)에서 나온 134건 중 **128건 구현**(6건은 근거 미확인·
환경 한계로 제외). 코드 주석 태그 `[970 · A-05]` 식으로 감사 번호를 그대로 쓴다. 감사 원문은
zip 밖(세션 스크래치)이라 아래 표가 요약이다.

## P1 — 버그 (7)

| # | 바뀐 것 |
|---|---|
| A-01·02 | 다크 모드에서 데스크탑 GNB 드롭다운·헤더 검색 드롭다운이 흰 판 위 밝은 글자로 **안 보이던** 것(인라인 흰 배경) → 테마 토큰 `popover-surface` |
| B-01 | /notes·/town 모바일 글쓰기 FAB 가 `fixed` 가 아니라 본문 흐름에 그려져 왼쪽으로 잘리던 것(`.njn-fab{position:relative}` 가 유틸리티를 이김) |
| B-02 | /region/[id]·단지 허브의 입주물량·정비사업이 **다른 도시의 "중구·서구"** 를 섞어 보여주던 것 — 시/도까지 포함한 조회 키 |
| B-03 | 비서울 지역 페이지의 상위 시/도가 "서울"로 고정(JSON-LD·"서울 전체 단지" 링크) — 카탈로그 역방향으로 시/도 결정 |
| C-01 | 모임 일시가 UTC 로 표시(9시간 차이) → KST 포맷터 `lib/format/kst.ts` |
| C-02 | 동네 글 등록 뒤 피드에 안 보이고(ISR 2분) 피드백 없음 → 즉시 재검증 + 상세로 이동 + "+NP 적립" 모먼트 |

## P2 — UX (65)

주요 항목: /subscription 게스트에게 "현재 이용 중" 대신 **"무료로 시작"**(A-06), 로그인 뒤 **고른 플랜 유지·강조**(A-07), 기간별 할인표 모바일 잘림(A-05), 플랜 문구 "PRO 의…"→"플러스의 모든 혜택"(A-08), **리포트 판매 수수료 단일 출처 7%**(A-09·C-15 — /legal/fees 10%/8% 표기가 7% 로 바뀜, 아래 확인 항목), 약관 시행일·개정 이력 v1.3(A-12), 가입·로그인 동의 문구에 약관 링크(A-13), 가입→환영→원래 화면 복귀(A-14), 헤더·메뉴 로그인이 현재 화면으로 복귀(A-15), 결제 확인 실패 화면에 고객센터 1차 CTA(A-16), /support 의 없는 기능 약속·응답시간 충돌 정리(A-17·18), 셸 링크 프리페치 24건/페이지 → 0(A-19), 쿠키 배너가 요금제 버튼 덮음(A-20), 빌링 게스트 요약(A-21), 결제 방식·해지 문구 3갈래 통일(A-22), `border-border` 유틸 부재로 62곳 테두리가 currentColor(B-05), 다크 `text-surface` 21곳 판독 불가(B-06·A-04), 노트 저장 바가 탭바 없는 화면에서 76px 떠 있음(B-07), 맨 위로 버튼이 저장 바·액션 바와 겹침(B-08·09), **새 노트가 현장 체크 9항목·만족도 기본값으로 저장되던 것**(B-10 — 미입력은 미입력으로), 저장 바 오류 표시·상단 사진 진행 표시(B-11·12), 비로그인 편집 404→로그인(B-13), 비교 noteId 소실(B-14), "카드 보기/카드로 보기" 혼동(B-15), AI 재료 "실거래 N건"이 개월 수(B-16), 단지 "노트" 탭 정체성(B-17), /complex/tx 소프트 404(B-18), 지도 목록 헤더 가림(B-19), 카테고리 줄 활성 카드 화면 밖(B-20), /data/records 버튼 잘림(B-21), /my/settings 게스트에 탈퇴·로그아웃 노출(C-03), 뉴스 바이라인 UTC(C-04), 다크 사진 커버 62% 어둡게·Lab 배지·brand-photo-chip(C-05·06·07), 키워드 알림 입력 30px(C-08), **DB 원문 오류 7곳 화면 노출 제거**(C-09), 프로필 주소 `/@닉네임` 404(C-10), Lab 노트 "직접 방문" 오표기(C-11), 뉴스 히어로 빈 상자(C-12), 전문가 0명 "0 · 0 · —"(C-13), 모임 만들기 401 입력 소실(C-14), /seller 고아 페이지 → /creators(C-16), 동네 홈 100곳 시·도 그룹 인덱스(C-17), 다크 Q&A 테마(C-18), 유사 기사 회색 상자(C-19), 합니다/해요 혼용 11곳(C-20), 정렬 설명 반대(C-21), "0개 표시 중 · 마지막이에요"(C-22), 뉴스 지역 칩 단위 혼재(C-23), 폼 입력 label(C-27), Q&A 머리 구조 통일(C-28), 탭바 "동네" 활성 매칭(C-29), `<title>` 접미 4종 → `| 내집나우` 46파일·description 누락 4곳·/legal canonical(A-31·C-25·C-26).

## P3 — 마감 (56)

홈 티저 중복(A-23), 고객센터 고아 칸·빈 공지(A-24·25), 메뉴 라벨 말줄임(A-26), 포커스 링 이중(A-27), 다크 구분선(A-28), 모바일 푸터 위 130px 공백(A-29), 오프라인 문구(A-30), 법적 문서 날짜 형식(A-32), 포인트 h1(A-33), 비교표 sticky 겹침(A-34), /safety h1·비활성 버튼(A-35), 고지 폭(A-36), /partners 폭(A-37), 오픈 알림 게스트(A-38), FAB vs 쿠키 배너(A-39), 브레드크럼 nav(A-40), 비밀번호 화면 공백(A-41), 노트 작성 h1·퀵모드 라벨·마켓 커버 크롭·빈 링크 정리(B-22~25), 비로그인 401 콘솔(B-26·C-43), 단지 선택 라벨 중복·placeholder 잘림·한도 문구(B-27~29), 순위 막대 라벨(B-30), 억 소수 자릿수 통일(B-31), 진행 중인 달 "집계 중"(B-32), 리포트 지역 목록 같은 시/도 우선(B-33), /tx 214개 지역 시/도 접기(B-34), D-day 줄바꿈·경매 다크 테마(B-35·36), 지도 폴백 버튼·배너 문구(B-37·38), 시세 탭 지역 전달·내 노트 링크·전월세 포맷·면적대 패딩·브라우즈 실패 표시·/supply h2·비교 빈 상태(B-39~45), /town "오늘 새 글 0"·필터 초기화 pushState·장소 미정 지도·내비 라벨·댓글 더 보기·서버 오류 문구·카테고리 카드 Link/설명·모임 빈 상태 CTA·매물 빈 상태 분기·**공용 GuestGate 5화면**·프로필 팔로우/전체보기/아바타·h1 안 배지·청약 env 문구·프롬프트 인덱스(C-30~46), `relative-time` 절대 날짜 KST(I4 보고 → 오케스트레이터).

## 제외 (6)
- A-09 의 "수수료 단일 출처" 는 구현했으나 **정책 값은 소유자 확인 필요**(아래).
- B-46(인덱스 key) 감사 결과 결함 아님. C-45 는 A-10 과 동일. A-31/C-25 의 "루트 title.template" 은 Next 15.5 의 레이아웃 title 규칙상 접미가 이중/누락되므로 `buildPageMetadata` 방식으로 통일(I5 판단). C-24 는 I1 sweep 에 포함. B-20 은 I4 가 처리.

## 소유자 확인 항목
1. **리포트 판매 수수료**: 코드가 실제로 정산하는 7%(`lib/creator/sales.ts`)를 단일 출처로 삼아 `/legal/fees` 의 10%(리포트)·8%(전자책) 표기를 **7%** 로 맞췄다. 10% 가 의도한 정책이면 `lib/billing/marketplace-fees.ts` 의 상수 하나만 바꾸면 된다.
2. 이용약관 개정 이력 v1.3 시행일을 2026-09-06 으로 적었다 — 실제 고지일에 맞게 바꿔도 된다.
3. 셸 링크 `prefetch={false}` 는 데스크탑 GNB 의 호버 프리페치도 끈다(Next 15.5 동작). 즉시 전환감이 아쉬우면 `Header.tsx` 상단 4개 링크만 되돌리면 된다.

## 검증
- `npm run build` 전체 게이트 통과 — 단위검증 **409**(969: 378 → +31), 번들 예산 6 라우트 통과(/analysis 488/490 여유 2KB — 다음 라운드 재조정), JSON-LD 290 페이지, 캐시 정책(동적 패턴 23건 — `/complex/tx` 등록), 리다이렉트 맵, 링크 크롤 **289/289**.
- Playwright(Pixel 5·1280): 다크 GNB/검색, /subscription 게스트 "무료로 시작"·할인표 12개월·-21% 표시, /notes FAB `position:fixed`, /tx 시·도 접기(14,000px → 3,376px), /town "오늘 새 글 0" 숨김, /town/experts "모집 중", 키워드 입력 331px, 노트 저장 바 bottom 12px, /support 응답시간 통일, /my·/my/settings 게스트 게이트, 체크아웃 게스트 미리보기 — 페이지 오류 0, 가로 오버플로 0.
