# 968 — 모바일 최적화 47건 + 토스 도메인 전환(결제창 심사) 7건 (2026-09-06)

소유자 요청: "모바일 버전도 최적화 방안 50가지 분석·제안 → 터미널로 적용할 수 있게",
"토스페이먼츠 도메인 변경이 '결제창과 연동이 안 되어 있다'로 거절 — naezipnow 에서 결제되게
업데이트·최적화·개편". 번호는 `docs/../모바일-최적화-50-제안.md` 의 제안 번호와 코드 주석
`[968 · N]` 태그가 같다(토스 트랙은 `[968 · T1~T7]`).

근거 데이터(web_vitals 14일, 모바일): LCP p75 2,361ms · INP 184ms · `/complex/[id]` LCP 3,009ms ·
TTFB 792ms · 홈 INP 568ms. 제안 50건 중 **47건 구현**, 3건 보류(아래 "보류" 절).

## T. 토스페이먼츠 — naezipnow.com 결제창 심사 대응 (7)

| # | 바뀐 것 |
|---|---|
| T1 | **비로그인 방문자에게도 결제위젯이 보인다** — 예전엔 `/subscription/checkout` 이 주문 생성(401)에서 멈춰 심사자가 결제창을 못 봤다. 이제 게스트는 주문 없이 위젯(결제수단·약관)을 미리 보고 "로그인하고 결제하기" 로 로그인 뒤 같은 화면으로 돌아온다(서버 주문은 로그인 뒤에만) |
| T2 | 주간권 버튼: 게스트를 로그인이 아니라 **결제 화면으로 먼저** 보낸다(월간·연간 자동결제는 로그인 우선 유지) |
| T3 | 결제 화면 법적 고지가 `/subscription` 과 같아짐 — 예전엔 결제 화면만 "모든 이용권 1회성" 이라고 적혀 있었다(자동결제 개통 상태와 불일치) |
| T4 | 모바일 체크아웃: 위젯 자리 높이 예약(스켈레톤) — 위젯이 뜰 때 결제 버튼이 점프하지 않음 |
| T5 | `/api/health?detail=1&token=…` 에 `toss` 진단 블록(키 모드 live/test·종류 widget/api·시크릿 유무·빌링 개통·웹훅 URL·사이트 origin — 값은 절대 안 나감) |
| T6 | `docs/toss-domain-change-review.md` — 재심사 신청 시 적을 값·상점관리자 체크리스트(웹훅 URL 교체 필수)·배포 후 자가 점검 |
| T7 | 결제 코드 전 범위에 `nuguzip.com` 잔존 0건 확인(successUrl/failUrl 은 현재 도메인 런타임 값) |

## A. 단지 페이지 (9)

| # | 바뀐 것 |
|---|---|
| 1 | 곁다리 조회 예산 분리 — 실거래(히어로)는 8초, 나머지 4종·섹션 7종은 **5초** 상한(예전 섹션은 상한 없음 → 최대 45초 붙들 수 있었음). Suspense 는 ISR 미스가 비스트리밍이라 효과 없어 미적용(이유는 page.tsx 주석) |
| 2 | 767px 이하 첫 화면 블록(`.fold`) 리빌 애니메이션 제거 — 텍스트가 투명으로 시작하지 않음(홈·단지) |
| 3 | 하이드레이션 직후 XHR 감축 — 관심 여부는 세션이 있을 때만·단지별 30초 공유, 리뷰는 뷰포트 진입 시(게스트 첫 화면 −2건, 액션바 등장마다 −1건) |
| 4 | `/complex/[id]` 번들 예산 등재(470KB) — 실측 486→**461KB**; 차트 서버 컴포넌트화, 시세 탭·내 기록 탭 `dynamic()` |
| 5 | 로딩 스켈레톤을 실제 레이아웃(네이비 히어로·KPI 6칸·탭)과 맞춤 |
| 6 | 전월세 표: 767px 이하 월별 카드(가로 스크롤 제거) |
| 7 | 화면 밖 섹션 `content-visibility:auto` |
| 34 | 탭 칩·행동 알약 44px 터치 타깃 |
| 46 | 관심 등록 성공 토스트에 "푸시로 받기"(권한 요청은 탭 안에서만, 자동 프롬프트 없음) |

## B. 홈 (7)

| # | 바뀐 것 |
|---|---|
| 8 | 모바일·데스크탑 두 벌 섬 중 **안 보이는 쪽은 타이머·fetch·옵저버를 돌리지 않음**(360px: 타이머 −2, 옵저버 −2, 게스트 요청 −1, 로그인 요청 −3) |
| 9 | 히어로 칩·제안 `<button router.push>` → `<Link>`(프리페치) + 검색 폼 `enterKeyHint=search` |
| 10 | 미니지도: 보이고 나서 유휴 시점에만 SDK 로드(그 전엔 "지도 열기" 링크) |
| 11 | RevealOnScroll: `data-reveal` 없는 페이지는 옵저버 미설치, 범위 `<main>`, 처리 끝나면 해제 |
| 19 | 저속망(saveData·2g/3g) 프리페치 끔 + 하단 카드 19개 `prefetch={false}` |
| 38 | 티커·오늘의 한 줄: 터치·포커스 시 정지(3초 유예), 터치 기기는 기본 정지 레일, 일시정지/재생 버튼 |
| 39 | 베타 안내: 전면 모달 → 본문 상단 한 줄 배너(쿠키 배너·설치 배너와 겹치지 않음) |

## C. 셸·CSS (9)

| # | 바뀐 것 |
|---|---|
| 12 | 스크롤 리스너 3개 → 1개(rAF) · 헤더 축소를 높이 변경 대신 `transform: scale(.96)` (문서 리플로우 제거) |
| 13 | 767px 이하 글래스 blur 22→12px·saturate 제거, 배경 블롭 애니메이션 정지·크기 절반 · `deviceMemory≤4` 기기는 블러 끔 |
| 14 | 루트 레이아웃 조건부 섬 4개(PullToRefresh·BrandSplash·DragScroll·iOS 설치 힌트)를 조건 판정 뒤에만 로드 — 모바일 26→22 |
| 15 | AdSense: 1024px 미만은 `load`+유휴 뒤 주입, 데스크탑은 `fetchpriority=low`(메타·ads.txt 그대로) |
| 28 | 767px 이하 입력 글자 1rem(16px) — iOS 포커스 확대 방지 |
| 30 | `viewport.interactiveWidget=resizes-content` + 키보드 열림 시 고정 저장 바·액션 바 숨김 |
| 34 | 탭바 46px 타깃, `--nz-tabbar-offset` 64→68px |
| 40 | 동의 전에도 탭바 유지 — 쿠키 배너를 탭바 위 컴팩트 형태로 |
| 41·47 | 데스크탑 GNB·검색 효과는 768px 이상에서만 등록 · 전체 메뉴에 "홈 화면에 추가"(Android prompt / iOS 안내) |

## D. 지도 (5)

| # | 바뀐 것 |
|---|---|
| 23 | SDK 키를 서버에서 prop 으로(직렬 fetch 1단계 제거), `oapi.map.naver.com`·`nrbe.pstatic.net` preconnect, sdk-config 브라우저 캐시 1h |
| 24 | 드래그 시작 시 검색·필터·레이어를 칩 한 줄로 접고 1.2초 유휴 뒤 복귀(필터 열림·입력 중엔 잠금) |
| 25 | 목록 뷰 40개 + "더 보기"(현재 데이터 상한 30이라 대기 상태) + `cv-auto` |
| 26 | 검색 입력 `enterKeyHint=search`·자동완성 끔, 지도 루트 `touch-manipulation` |
| 27 | 설치 앱 당겨서 새로고침: `/map` 비활성, `[data-ptr-ignore]`(지도·캐러셀·바텀시트) |

## E. 폼·터치·이미지 (9)

| # | 바뀐 것 |
|---|---|
| 17 | 피드 첫 카드 `priority`(eager·high), 카드 `sizes` 100vw, 노트 캐러셀 `/_next/image` srcSet(1600px 원본 → 최대 1080w AVIF/WebP) + 첫 장 high |
| 18 | 사진 리사이즈를 Worker(OffscreenCanvas)로 — 미지원·실패 시 기존 메인스레드 경로 |
| 21 | `/notes/new/loading.tsx`(탭바 + 누르면 즉시 스켈레톤) |
| 29 | `enterKeyHint`/`inputMode`/`autoComplete`(로그인·가입·비밀번호·검색·문의·노트) |
| 31 | 인증·온보딩 화면 `min-h-dvh` |
| 32 | 노트 작성 "촬영" 버튼(`capture=environment`, 터치 기기만) — 퀵모드 첫 행동 |
| 35 | iOS 에서도 실제로 잠기는 스크롤 잠금(`useScrollLock`: 모달·전체 메뉴·캐러셀) |
| 36 | 바텀시트 핸들 + 아래로 60px 스와이프 닫기 · 전체 메뉴 오른쪽 스와이프 닫기 |
| 37 | 캐러셀 스와이프 축 판정(`|dx|>1.5|dy|`), 가운데 탭 확대 |

## F. PWA·관측·QA (6)

| # | 바뀐 것 |
|---|---|
| 42 | 오프라인 페이지 CSS 인라인 + SW 가 오프라인 페이지의 자산도 프리캐시 |
| 43 | SW 정적 자산 cache-first(`/_next/static`·아이콘·폰트, 150개 상한, 버전 캐시 정리) |
| 44 | 매니페스트 `id`·`display_override`·스크린샷 2장(실제 홈 캡처), `orientation` 고정 제거 |
| 45 | 푸시 배지 단색 아이콘 `badge-96.png` |
| 49 | INP 하위 구간(입력 지연·처리·표시·loadState) 기록 — 홈 568ms 원인 분해 가능 |
| 50 | Lighthouse 대상에 단지·노트 페이지 추가, LCP 예산 4000→3000·TBT 300, Playwright `mobile`(Pixel 5) 프로젝트 + 가로 오버플로·탭바 44px 자동 검사 |

## 보류 (3) — 다음 릴리스 후보
- **16 폰트 셀프호스팅** — 폰트 바이너리 반입·서브셋 전략이 필요해 이번 zip 범위 밖.
- **20 노트 상세 ISR 전환** — 소유자·플랜·댓글 분리 설계가 큰 변경(정확성 우선).
- **22 미들웨어 getUser 조건화** — 인증 경로라 별도 검증 창이 필요.
- 48 은 967(항목 33)에서 이미 수정.

## 판단 사항(소유자 확인)
- 홈 베타 안내가 모달→인플로우 배너라 재방문(미닫힘) 시 CLS 약 0.03~0.05 가능(현재 0, 기준 0.1 이내).
- AdSense 스크립트 태그가 HTML 에 정적으로 실리지 않고 인라인 부트가 주입한다(메타·ads.txt 는 그대로) — 애드센스 "사이트 연결" 은 메타로 확인된다.
- 단지 페이지 곁다리 예산 5초: DB 가 5~8초로 느린 창에서는 반쪽 캐시 대신 5xx(비캐시)를 낸다.
- 매니페스트 스크린샷은 로컬(외부 지도 차단)에서 찍어 미니지도 자리에 폴백 카드가 보인다 — 운영에서 다시 찍어 교체 가능.

## 검증
- `npm run build` 전체 게이트 통과 — 단위검증 **365**(967: 292 → +73), type-ramp 11=11, dead-controls·route-links·icon-names·contrast·import-hygiene·redirect-map·plan-labels·review-freeze 통과, tsc·eslint 0.
- 번들 예산: / 481KB(495) · /map 347(375) · /analysis 484(490) · /analysis/ai 458(480) · /notes/new 461(470) · **/complex/[id] 467KB(470, 신규 등재 — 여유 3KB, 다음 릴리스에서 예산 근거 재조정 권장)**.
- 링크 크롤 196/196. Playwright `mobile`(Pixel 5) 프로젝트: `/`·`/notes`·`/town`·`/subscription`·`/login` 가로 오버플로 0, 하단 탭바 터치 타깃 ≥44px 통과(9 pass, 1 flaky→재시도 통과).
- 로컬 실측(더미 위젯 키로 빌드): 비로그인 `/subscription/checkout?tier=pro&billing=weekly` 에 요약(1,100원·9월 13일까지·주문번호 "로그인 후 발급")·"결제하려면 로그인이 필요해요"·**"로그인하고 결제하기" → `/login?callbackUrl=%2Fsubscription%2Fcheckout%3Ftier%3Dpro%26billing%3Dweekly`** 확인. 위젯 자체는 컨테이너에서 토스 SDK 를 못 받아 "불러오지 못했어요" 분기로 떨어짐 — 운영에서는 SDK 가 열리므로 배포 후 시크릿 창에서 확인.
- 홈(모바일) 스크린샷: 베타 한 줄 배너·탭바·검색 폼 정상, 페이지 오류 0.
- 참고: `/subscription` 모바일에서 간헐적 React #418(하이드레이션 불일치)이 **967 기준 빌드에서도 같은 빈도(6회 중 1회)** 로 재현됨 — 968 유입 아님, 다음 릴리스에서 원인 추적(시간 의존 렌더 의심).
