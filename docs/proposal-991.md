# 991 — 대규모 개편안: 넓히지 않고 깊게 판다

작성 2026-09-12 · 근거: 운영 DB 실측(2026-08-13~09-12) + 코드 전수 조사 (라우트 158 · API 311 · 컴포넌트 128 · globals.css 5,041줄)

## 0. 한 줄 요약

**158개 화면을 지난 30일 동안 31명이 열었고, 결제는 0건이다.** 지금 필요한 대규모 개편은 "더 많이"가 아니라
**화면을 절반으로 줄이고, 남은 절반을 두 배 깊게 만들고, 결제 한 줄을 끝까지 통과시키는 것**이다.

## 1. 진단 — 숫자가 말하는 것

### 1-1. 규모 대 사용

| 항목 | 값 | 출처 |
|---|---|---|
| 사용자 화면 | **158** (순수 리다이렉트 8 포함) | `app/**/page.tsx` |
| API 라우트 | **311** (크론 42 · 관리자 25) | `app/api/**/route.ts` |
| 컴포넌트 | **128** (그중 `ui/` 킷 8종은 사용 0) | `app/components`, `components/` |
| globals.css | **5,041줄** · 커스텀 속성 163 · 키프레임 62 | — |
| 가입자 | **5명** (무료 4 · 엔터프라이즈 1=관리자) · 최근 30일 가입 2 | `app_users` |
| 30일 방문자 / 세션 / 조회 | **31 / 82 / 658** | `page_view_events` |
| 임장노트 | 30건 · 작성자 4명 · 공개 27 | `inspection_notes` |
| 동네이야기 글 | 932건 — **전부 자동 생성, 사람 글 0** | `board_posts.is_automated` |
| AI 분석 실행 | 누적 24회 | `ai_analysis_runs` |
| 전문가 프로필 · 모임 참여 · 채팅 · 매물 · 리포트 구매 | **전부 0** | 각 테이블 |
| 결제 승인 | **0건** (시도 11 = 미완료 7 · 실패 4) · 자동결제 등록 3건 전부 `pending` | `payments`, `billing_subscriptions` |

### 1-2. 어디에 머무는가 (30일 · 조회 상위)

| 화면 | 조회 | 평균 체류 | 읽기 |
|---|---|---|---|
| `/` | 165 | **279초** | 홈이 곧 서비스 |
| `/analysis` | 62 | **251초** | 가장 오래 읽는 화면 |
| `/town` | 31 | 7초 | 들어오자마자 나간다 |
| `/map` | 30 | 46초 | 쓰인다 |
| `/subscription` | 25 | 20초 | 관심은 있다 |
| `/complex/[id]` | 22 | 22초 | 38,510개 단지 중 22회 |
| `/notes` | 21 | 4초 | 피드가 안 읽힌다 |
| `/notes/[id]` | 13 | 40초 | 노트 한 건은 읽힌다 |
| `/auctions` | 12 | 46초 | 의외로 읽힌다 |
| `/subscription/billing` | 8 | **87초** | 카드 등록에서 헤맨다 |
| `/analysis/ai/*` | 11 | 8초 | 도구 화면은 안 머문다 |
| `/town/groups` `/town/library` `/supply` `/town/news` `/qna` | 8~14 | **1~3초** | 즉시 이탈 |

### 1-3. 퍼널

- 가입 화면 조회 **179** → 제출 2 → 완료 **3** (≈1.7%)
- 로그인 성공 10 · 실패 **8**
- 오픈 알림(결제 준비 중일 때) 클릭 **16** → 지불 의사는 있었다
- PWA 설치 안내 노출 95 → 설치 2

### 1-4. 기기별 실측 (Core Web Vitals p75, 30일)

| 기기 | LCP | INP | CLS |
|---|---|---|---|
| iPhone | 1.3초 ✓ | **752ms ✗** (홈 1,296ms) | 0.01 ✓ |
| Android(갤럭시) | 2.6초 △ | 106ms ✓ | 0.01 ✓ |
| 데스크톱 | **7.5초 ✗** (good 45%) | 64ms ✓ | 0.01 ✓ |

- 아이폰의 문제는 **반응 지연**이다. 홈이 모바일·데스크톱 트리를 **둘 다** 렌더하고(각 290줄·412줄, 같은 컴포넌트 13개를 두 번 마운트), `rise-in` 애니메이션 780곳, 뷰포트 추적 이벤트가 6명에게서 30일 5,198건 발생한다.
- 데스크톱의 문제는 **첫 그림**이다. `/analysis` 5.8초 · `/redevelopment` 5.3초 · `/qna` 5.2초.

### 1-5. 결제 레일

코드에 **6개 레일**이 살아 있다: 토스 단건(위젯) · 토스 빌링 · 카카오페이 · Stripe · 토스페이(앱인토스, 닫힘) · 앱 IAP(서버만). 서로 다른 상품을 팔고(주간권은 토스 단건만, 월간·연간은 빌링/카카오/Stripe), 각각 별도의 웹훅·환불·컴플라이언스 표면을 갖는다. 그 여섯 개로 지금까지 승인된 결제는 0건이다.

## 2. 개편 원칙

1. **쓰이는 것만 앞에** — 30일 체류 10초 미만 화면은 앞에서 치운다(보관 또는 병합).
2. **하나의 정체성** — "임장노트 × AI 분석으로 단지 하나를 깊게". 매물·개발물건·전문가·모임은 정체성이 아니다.
3. **결제는 한 줄** — 레일 하나, 플랜 하나(플러스), 진입 하나(AI 결과 직후).

## 3. A. 화면 간결화

### A1. 정보구조 158 → 약 95 (1차) → 약 70 (2차)

| 묶음 | 지금 | 후 | 조치 |
|---|---|---|---|
| 비교 화면 | 6 (`/analysis/compare` `/complex/compare(+slug)` `/notes/compare` `/listings/compare` `ai-compare`) | 1 + 노트 상세 내 회차 비교 | 병합. `/complex/compare/[slug]` SEO 링크는 쿼리로 이관 |
| 실거래 목록 | 5 (`/tx*` 3 · `/complex/browse` · `/complex/tx/[slug]`) | 2 | `/tx*` → `/complex/browse?…` 리다이렉트 |
| 커뮤니티 피드 | 5 (`/town` `/notes` `/town/library` `/qna` `/town/prompt`) | 2 (`/town` 뉴스+공개노트 · `/notes`) | 사람 글 0건 — 피드는 뉴스+노트로 재정의 |
| 시장 온도·타이밍 | 4 (`timing` `temperature(+region)` `cycle`) | 1 | 같은 주간 스냅숏 |
| 계산기 | 5 (`/calculator*`) | 0 (분석 도구 안 탭) | SEO 리다이렉트 유지 |
| 리포트 | 8 (월간·지역·주간 3계열) | 4 | `/digest*` → `/reports` 아래 |
| 모임·채팅 | 3 | 0 (보관) | 참여 0 |
| 전문가 | 5 | 0 노출 (보관) · `/my/leads`+`/my/consultations` → 1 | 프로필 0 |
| 개발물건 | 6 | 0 노출 (보관) | 협력업체 0 |
| 매물 | 4 | 2 (`/listings`, `[id]`) | 비교함·등록은 지도 안으로 |
| `/my/*` | 17 | 7 | 관심단지+관심매물+저장검색 → `관심`, 미션+추천 → `포인트`, `analyses` → `기록`, `assets`·`expert-profile` 삭제 |
| 데이터 문서 | 4 | 2 | `/data/records` → `/data-sources`, `/developers` 유지 |
| 노트 출력 | 3 (`card` `deck` `print`) | 1 (`card`) | 인쇄는 CSS `@media print` |
| 임베드 | 3 | 2 | `/widget` 생성기는 단지 상세 안으로 |
| 검색 | 3 | 2 | `/recommend` 삭제 (진입 1곳) |
| 순수 리다이렉트 페이지 | 8 | 0 | `redirect-map.ts` 로 이동 |

보관(unlist)은 라우트를 남기되 내비·사이트맵·홈에서 뺀다. 삭제 전 30일 관찰.

### A2. 홈 — 트리 하나, 블록 다섯

- **모바일·데스크톱 이중 트리 폐지.** 하나의 트리를 CSS 그리드로 재배치한다. 13개 컴포넌트 이중 마운트, `useShellActive` 훅, `shell=` prop 13곳이 사라진다. → 아이폰 INP의 직접 원인 제거.
- 비로그인 첫 화면 = ① 검색 한 줄 ② 오늘의 시장 한 문장 ③ 공개 임장노트 3장 ④ AI 분석 입구 1개 ⑤ 지역 시세 카드. 그 아래는 없다.
  - 사라지는 것: 티커(체류에 기여 없음) · 슬로건 띠 · 미니지도(지도 탭이 있다) · 도구 추천 · "더 알아보기" 8링크 · 동네이야기 입구(7초 이탈).
- 로그인 첫 화면 = 위 5 + `내 관심`(변동) + `이어 쓰기`. 출석·미션·레벨 KPI 는 `마이`로.
- 뷰포트 추적(`ViewportGroupTracker`) 삭제 — 5,198건/6명은 측정이 아니라 잡음이다.

### A3. 탭바·헤더

- 탭바 5칸 유지, **'동네' → '분석'**. 근거: `/analysis` 62회·251초 vs `/town` 31회·7초. 홈·지도·**기록(+)**·분석·마이.
- 햄버거 메뉴 43링크 → **12**. 서비스 12개 묶음 삭제, 계정 9 → 4, 지원 4 → 2.
- 데스크톱 GNB 4드롭다운 18링크 → 4 대분류 + 각 2~3.
- 헤더 CTA "노트 쓰기" 유지.

### A4. 디자인 시스템 정리 (globals.css 5,041 → 3,000 이하)

| 대상 | 지금 | 조치 |
|---|---|---|
| 죽은 UI 킷 | `Card` `Badge` `SectionHeader` `SourceNote` `WaveLoader` `BarLoader` `CardSection` `ChipRow` 사용 0 | 삭제 |
| 스켈레톤 2계열 | `.skeleton`(185) · `.sk`(31) · `LoadingHint`(7) · `BrandLoader` 5종 | `.skeleton` 하나로 |
| 토큰 미사용 | `--sp-*` 6 · `--z-*` 6 참조 0 · `--radius/--fs/--dur` TSX 참조 0 | 코드모드로 채택하거나 삭제 |
| 하드코딩 크기 | `text-[…]` 2,734 · `rounded-[…]` 855 · `bg-[…]` 416 · z-index 12종 | `text-[12px]`(867)·`[13px]`(696)·`[10px]`(328) → `t-sub/t-body/t-caption` 일괄 치환 |
| 모션 언어 3계열 | 15a-d · 19a-e · `njn-*` | `--dur/--ease` 하나 + `rise-in` 하나 |
| 컴포넌트 루트 2곳 | `app/components/` · `components/` | 하나로 |
| 죽은 설계 모듈 | `lib/design/tokens.ts`(경쟁 팔레트 `#3182F6`) `field-tokens.ts` `category-workflow.ts` `use-viewport-tier.ts` | 삭제 |
| 죽은 헤더 | `x-woodong-shell` 항상 `"desktop"`, 읽는 곳 0 | 삭제 |
| 주석 드리프트 | `enableSystem` 서술 2곳 · 브레이크포인트 서술 1곳 | 수정 |

### A5. 데스크톱

- **고정 좌측 내비 도입** (지금은 `/admin` `/legal` 만 있다). 1240px 컨테이너 안에 220px 내비 + 본문. 헤더 드롭다운은 사라진다.
- 사이드바 폭 6종(300~400px) → **340px 하나**. `lg:grid-cols-[minmax(0,1fr)_340px]` 유틸 하나로.
- 태블릿(768~1279)이 데스크톱 레이아웃을 통째로 받는다 → `lg`(1024) 를 2열 시작점으로, `md` 는 1열+넓은 여백.
- LCP: `/analysis` `/redevelopment` `/qna` 첫 화면 이미지·폰트 우선순위, 위 A1 병합으로 두 화면은 사라진다.

### A6. 아이폰 · 갤럭시 실측 대응

| 기기 | 실측 | 조치 |
|---|---|---|
| iPhone | 홈 INP 1,296ms | A2 단일 트리 · 뷰포트 추적 삭제 · `rise-in` 관찰자 홈에서 제거(첫 화면은 정적) · 첫 화면 클라이언트 섬 수 13 → 3 |
| iPhone | 상태바·카메라 섬 | 988a 완료 — 소유자 실기기 확인 대기 |
| iPhone | 입력 포커스 확대 | 완료(16px 강제) |
| Galaxy | LCP 2.6초 | 히어로 검색창 위 이미지 없음 확인 · 폰트 `display=swap` · 첫 화면 클라이언트 섬 0 |
| Galaxy | 삼성 인터넷 다크모드 강제 | `.dark{color-scheme:dark}` 만 있고 `<meta name="color-scheme">` 없음 — 삼성 인터넷 실기기 검증 후 메타 추가 |
| Galaxy | 3버튼 내비 하단 | `env(safe-area-inset-bottom)` 는 iOS 전용 — 탭바 하단 여백 8px 고정 추가 |
| 공통 | 터치 40/24px | 989 게이트 유지 · 병합 후 전 화면 재측정 |

### A7. 로딩 · 오류 경계

- `error.tsx` 1/177 → 영역별 6개(홈·지도·노트·분석·구독/결제·마이). 결제 화면이 루트 경계로 떨어지는 지금 상태는 위험하다.
- `loading.tsx` 25 → 조회 상위 15 화면 전부.

## 4. B. 기능 특화 · 심화 · 다양화

### B1. 특화 — 정체성 한 줄

**"직접 걸어본 단지를, AI가 같이 판단한다."**
임장노트(기록)와 AI 분석(판단)이 단지 상세라는 한 화면에서 만난다. 나머지 — 매물·개발물건·전문가·모임·리포트 판매 — 는 보관.

### B2. 심화 (핵심 루프를 두 배로)

| # | 항목 | 지금 | 후 |
|---|---|---|---|
| S1 | 단지 허브 | 5탭 + 9섹션 스택, 첫 화면 탭 11개 | **한 스크롤 4장**: 실거래 요약 → AI 진단 요약(무료 1회) → 이 단지 임장노트 → 주변. 탭 제거 |
| S2 | AI 도구 12 → **4 핵심** | 진단·예측·타이밍·임장동선은 이미 `CORE_AI_TOOL_IDS` | 나머지 8은 진단 결과 안 "더 물어보기"로. 허브 카드 22 → 4 |
| S3 | 노트 → 판단 | 노트에 `verdict` 필드가 있으나 화면 비중 낮음 | 저장 직후 **판단 카드**(살까·보류·패스 + 근거 3줄)를 AI가 제안, 사용자가 고른다 |
| S4 | 노트 회차 | `/notes/compare` 별도, 재방문 0건 | 노트 상세 안 "이 단지 다시 왔을 때" 버튼 → 이전 노트 자동 프리필 |
| S5 | 관심단지 변동 | 홈 레일 + `watchlist-new-tx` 크론 존재 | 노트를 쓴 단지는 **자동 관심** → 신고가·신저가 푸시 |
| S6 | 지도 | 5,332줄 클라이언트, 매물·실거래·탐색 3모드 | **실거래 + 내 노트 핀** 기본. 매물 레이어는 토글 |
| S7 | AI 결과 근거 | `CitationBlock` 있음 | 모든 숫자에 출처 칩(실거래 n건·기준월) 필수 — 게이트로 고정 |

### B3. 다양화 (근거 있는 것만 · 유입에 직결)

| # | 항목 | 근거 | 내용 |
|---|---|---|---|
| D1 | 단지 페이지 SEO 품질 | 38,510단지 색인 중 30일 22회 조회 · 구글 랜딩 10 | 단지 상세 첫 화면을 검색 의도(시세·실거래·평형)에 맞춰 재작성, 제목·설명 템플릿, 내부 링크(같은 동 단지 6개) |
| D2 | 노트 공유 카드 → 유입 | 988 카드 스튜디오 완성 | 카드에 단지 페이지 링크 + OG, 카카오톡 공유 1탭. 카드 조회 → 단지 → 가입 퍼널 계측 |
| D3 | 개인 주간 다이제스트 | `weekly-digest` 크론은 전체용 · `watch_regions` 존재 | 관심 지역·노트 단지 기준 **내 주간 요약** 이메일/푸시 (동의자만) |
| D4 | 청약 캘린더 알림 | `/apply` 17회·3.5초 — 보고 바로 나감 | 이번 주 접수 단지에 "알림 받기" 한 버튼. 내용 없이 화면만 있는 상태를 바꾼다 |

넣지 않는 것: 새 수직(경매 심화·개발물건·B2B), 챗봇 확장, 커뮤니티 기능 추가 — 사람 글 0건.

## 5. C. 결제

### C1. 레일 6 → 1

- **남기는 것**: 토스 단건(위젯, 주간권) + 토스 빌링(월간·연간).
- **제거**: Stripe(체크아웃·웹훅·부스트) · 카카오페이(웹훅 없음) · 토스페이 앱인토스(이미 닫힘) · IAP 서버(클라이언트 0).
- 효과: 웹훅 2→1, 환불 경로 3→1, `/legal/fees` 수수료표 중 실제 청구 0인 6줄 삭제, `group-passes.ts` `boost` 삭제.

### C2. 상품

- 지금 6 SKU(무료·주간 1,100·플러스 월 2,900·연 27,600·프로 월 18,900·연 181,200).
- **프로(EXPERT) 숨김**: 혜택(전문가 등록·리포트 판매 우선)의 전제인 전문가가 0명. 판매 페이지에서 내리고 코드는 유지.
- 판매 = **플러스 주간 · 월간 · 연간** 셋. 요금제 카드 3 → 2(무료·플러스).

### C3. 결제 완주 (0 → 1)

- `/subscription/billing` 체류 87초 · 등록 3건 전부 `pending` = 카드 등록창에서 멈춘다. 원인 후보: `requestBillingAuth` 팝업 차단(iOS Safari) · 빌링 키(`live_ck_`) MID 불일치. **소유자 실결제 1건**으로 확정한다.
- 결제 화면 3종(체크아웃·빌링·성공) 문구 통일, 성공 화면에 "바로 AI 분석 1회" 버튼.
- 단건 해지가 고객센터 티켓(`CancelRequestButton`) — 단건은 해지 개념이 없다. 버튼 삭제, 환불만 남긴다.

### C4. 가입 퍼널 (1.7% → 10%)

- 가입: 이메일+비밀번호+동의 → **소셜 1탭 우선**(카카오·토스·구글), 이메일은 아래.
- 온보딩 5단계(`profile_region → budget → purpose → persona → demo`) → **1단계(관심 지역)**. 나머지는 첫 노트 작성 때 묻는다.
- 로그인 실패 8/18: 실패 사유를 화면에 적는다(존재하지 않는 계정 / 비밀번호 / 소셜 연결 계정).
- 가입 이유가 되는 순간에 가입을 요구: 노트 저장·AI 실행·관심 등록 직전(소프트 가입 4회 노출·1클릭 — 지금은 거의 안 보인다).

### C5. 페이월 위치

- 지금: 무료 AI 월 2회, 페이월 9곳. 누적 AI 24회 — 한도가 걸린 적이 없다.
- 후: 무료 = **AI 진단 3회(누적)**. 3회째 결과 화면 하단에 **"주간권 1,100원으로 7일 무제한"** 한 버튼. 구독 페이지로 보내지 않고 그 자리에서 위젯.
- `/subscription` 은 설명 페이지로 남고, 전환은 AI 결과 화면에서 일어난다.

### C6. 정리

- `PreOrderCta`(오픈 알림) — 결제가 열렸으므로 삭제.
- 관리자 결제 화면에 **MRR · 활성 구독 · 이번 달 신규** 3숫자 추가(지금은 키 상태와 최근 20건뿐).
- 포인트: 충전 부존재·미도입 **유지(LOCKED)**. 상점 5종 중 닉네임 효과 2종·시즌 배지는 유지, 매물 부스트·글 부스트는 매물·커뮤니티 보관과 함께 보관.

## 6. 단계 계획

| 판 | 기간 | 내용 | 측정 목표 |
|---|---|---|---|
| **991** | 1주 | C3 결제 완주 · C4 가입 퍼널 · A2 홈 단일 트리 · A3 탭바 · A6 iOS INP | 첫 결제 1건 · 가입 완료율 10% · 홈 iOS INP < 200ms |
| **992** | 2주 | A1 정보구조 1차(-58) · A4 디자인 시스템 GC · A7 경계 · C1 레일 단일화 · C2 상품 | 화면 ≤ 100 · globals.css ≤ 3,000줄 · 레일 1 · 데스크톱 LCP < 2.5s |
| **993** | 2주 | B2 심화 S1~S7 · A5 데스크톱 좌측 내비 | 노트→AI 진단 연결률 · 단지 상세 체류 22초 → 60초 |
| **994** | 2주 | B3 다양화 D1~D4 · A1 2차(→70) | 구글 랜딩 10 → 100/월 · 카드 공유 유입 계측 시작 |

각 판은 지금까지처럼 zip + 적용 명령으로 전달하고, 게이트(빌드·모바일 탭·빈공간·번들 예산)를 전부 통과한 상태로만 낸다.

## 7. 하지 않을 것

- 새 수직 서비스(법률 서비스 유형 — LOCKED · 개발물건 확대 · B2B)
- 포인트 충전·현금 교환 — LOCKED
- 결제 레일 추가
- 커뮤니티 기능 추가(사람 글 0건인 동안)
- 번들 예산 상향(구조로 푼다)

## 8. 소유자 결정 필요

1. 전문가 · 개발물건 · 모임 · 매물 — **보관(비노출)** 으로 갈지, 삭제할지.
2. 프로(EXPERT) 플랜 — 판매 페이지에서 내리는 데 동의하는지.
3. Stripe · 카카오페이 — 코드 제거에 동의하는지 (환경변수 키는 소유자가 직접 폐기).
4. 탭바 '동네' → '분석' 교체.
5. 991부터 순서대로 진행할지, 특정 판을 먼저 할지.
